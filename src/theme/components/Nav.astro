---
type Section = "index" | "posts" | "projects" | "resume";
interface Props {
	current?: Section | undefined;
}
const { current } = Astro.props;

const items = [
	{ id: "index", label: "index", href: "/" },
	{ id: "posts", label: "posts", href: "/posts/" },
	{ id: "projects", label: "projects", href: "/projects/" },
	{ id: "resume", label: "resume", href: "/resume/" },
] as const;
---

<nav class="flex gap-5 font-mono text-[12.5px]" aria-label="Primary">
	{
		items.map((it) => (
			<a
				href={it.href}
				aria-current={current === it.id ? "page" : undefined}
				class:list={[
					"transition-colors duration-150 hover:text-accent",
					current === it.id ? "nav-active text-fg" : "text-fg-dim",
				]}
			>
				{it.label}
			</a>
		))
	}
</nav>

<style>
	/* "·" prefix on active item — needs ::before, not expressible in utility classes. */
	.nav-active::before {
		content: "· ";
		color: var(--color-accent);
	}
</style>
